  META ORCHESTRATOR for any code change that should end in a commit (features, bug fixes, refactors), when no language-specific linter-driven-development plugin covers the repository's language.
  WHEN: User requests code work (implement, fix, add, refactor), mentions "@ldd"/"ldd", or runs a /{{.CmdPrefix}}-* command.
