Scan the codebase structure: vertical (`app/feature/{handler,service}.py`, one
package per feature) vs horizontal (`app/{handlers,services}/feature.py`, or one
`models.py`/`views.py`/`services.py` per layer)?
