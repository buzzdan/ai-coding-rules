then confirm the package
   still vets. Go files only — the gate verifies edges in `.go` files alone, so an
   edge in another language is unverifiable; report such docs as unwired instead of
   improvising.
